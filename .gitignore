# Node / Capacitor build artifacts (generated in CI, not committed)
node_modules/
www/
android/
dist/
.DS_Store

# Signing material — never commit the keystore or its properties
*.jks
keystore.properties

# Logs
*.log